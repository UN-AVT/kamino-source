---
pagetitle: "Kamino"
output: 
  html_document:
    theme: lumen
    css: ["../../../z-assemblers/assets/styles/content/kamino.css", "../../../z-assemblers/assets/icons/line-awesome/css/line-awesome.css"]
    df_print: paged
    mathjax: NULL
    code_folding: show
    include:
      in_header: "../../../z-assemblers/fragments/header.html"
      after_body: "../../../z-assemblers/fragments/footer.html"
    self_contained: false
    lib_dir: libs
---

```{r setup, include=FALSE}

knitr::opts_chunk$set(message=FALSE, warning=FALSE, echo = FALSE)
options(scipen = 999) ## To disable scientific notation

# Clear environment and memory
rm(list = ls(all.names = TRUE)) #will clear all objects includes hidden objects.
gc() #free up memrory and report the memory usage.

library(maps)
library(geojsonio)
library(sf)
library(rnaturalearth)
library(rnaturalearthdata)
library(tidyverse)
library(ggiraph)
library(ggdark)

```

<div class="activity">
MAPS  
</div>

# Meteorite Landings
## Dot Map

```{r, out.width = "60%", fig.align = 'center', fig.cap="Photo by Tengyart on Unsplash"}

knitr::include_graphics("archetypes/meteorite-landings/tengyart-unsplash.jpg", dpi=300)

```


> Sometimes the meaning in life hits you like a meteorite...\
--- Curtis Tyrone Jones

***


# Ingest the data
## Meteorite Landings


```{r, message=FALSE, warning=FALSE, echo=FALSE}

df = read.csv("archetypes/meteorite-landings/meteorite-landings.csv", header = TRUE, stringsAsFactors = FALSE)
df

```


# Dot map
## Meteor shape


```{r, message=FALSE, warning=FALSE, echo=FALSE, fig.width=13, fig.height=8}

df_sorted <- df[order(df$mass),]

# PLOT NATURAL EARTH BORDERS
ne_world <- ne_countries(scale = "medium", returnclass = "sf")
# class(ne_world)

# removes Antarctica
# ne_world <- filter(ne_world, iso_a3 != "ATA") 

# theme parameters
theme_opts <- theme(
  legend.position = "bottom",
  legend.title = element_blank(),
  axis.text = element_blank(),
  axis.line = element_blank(),
  axis.ticks = element_blank(),
  axis.title = element_blank(),
  panel.grid = element_blank(),
  panel.grid.major = element_blank(),
  panel.grid.minor = element_blank(),
  panel.background=element_rect(fill="white", colour="white"),
  panel.border = element_blank(),
  plot.background = element_blank(),
)

v1 <- ggplot(data = ne_world) +
  # map feature styling
  geom_sf(fill="white", color="#888888", stroke=0.5) +
  # dot parameters
  geom_point( data = df_sorted, aes(x=reclong, y=reclat, color=mass), shape=8, size=1.5) +
  scale_color_gradient(low = "yellow", high = "red", na.value = NA) +
  theme_bw() +
  labs(x=NULL,
       y=NULL,
       title = "Meteorite Landings", 
       subtitle="Color by mass") +
  theme_opts

girafe(ggobj = v1, width_svg = 13, height_svg = 7,
       options = list(opts_sizing(rescale = TRUE, width = 1.0)))

```


# Dark theme
## Using magnitude and alpha as a visual variable


```{r, message=FALSE, warning=FALSE, echo=FALSE, fig.width=13, fig.height=10}

# theme parameters
theme_dark_opts <- theme(
  legend.position = "bottom",
  legend.title = element_blank(),
  axis.text = element_blank(),
  axis.line = element_blank(),
  axis.ticks = element_blank(),
  axis.title = element_blank(),
  panel.grid = element_blank(),
  panel.grid.major = element_blank(),
  panel.grid.minor = element_blank(),
  #bpanel.background=element_rect(fill="white", colour="white"),
  panel.border = element_blank(),
  #plot.background = element_blank(),
)

v2 <- ggplot(data = ne_world) +
  # map feature styling
  geom_sf(fill="transparent", color="#444444", stroke=0.5) +
  # dot parameters
  geom_point( data = df_sorted, aes(x=reclong, y=reclat, color=mass, alpha=mass), shape = 8, size = 1.5) +
  scale_color_gradient(low = "yellow", high = "red", na.value = NA) +
  scale_alpha_binned() +
  labs(x=NULL,
       y=NULL,
       title = "Meteorite Landings", 
       subtitle="Color and alpha by mass") +
  dark_theme_bw() +
  theme_dark_opts

girafe(ggobj = v2, width_svg = 13, height_svg = 10,
       options = list(opts_sizing(rescale = TRUE, width = 1.0)))

```



```{r, echo=FALSE}

# ggsave(filename = "assets/meteorite-landings.svg", plot = v1, width=13, height=10)

```


# References
## The citations and data sources used for this case

Earthquake Catalog, https://earthquake.usgs.gov/earthquakes/search/  
NOAA Satellite and Information Service, https://www.ngdc.noaa.gov/hazel/view/hazards/earthquake/event-data?minDeaths=140000  
Our World in Data, What were the world’s deadliest earthquakes? https://ourworldindata.org/the-worlds-deadliest-earthquakes  


```{r generateBibliography, echo=FALSE, eval=TRUE, message=FALSE, warning=FALSE}

# cleanbib()
# read.bibtex(file = "./citations.bib")

```



```{js, message=FALSE, warning=FALSE, echo=FALSE}

// Must be included to position footer
$(function() {
  $('.main-container').after($('.footer'));
})

```

