# Required libraries
#NA

####################################################################################
# Find If Data is Logical DataType
f_logical <- function(df_logical) {
  is.logical(df_logical)
}

# Call the function with your values to the parameters
f_logical(df$Eccentricity)
####################################################################################
