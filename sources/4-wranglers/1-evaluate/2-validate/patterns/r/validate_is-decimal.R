# Required libraries
library(schoolmath)

####################################################################################
# Find If Data is Decimal 
f_decimal <- function(df_decimal) {
  is.decimal(df_decimal)
}

# Call the function with your values to the parameters
f_decimal(df$Eccentricity)
####################################################################################
