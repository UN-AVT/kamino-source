---
title: "Kamino"
output:
  bookdown::gitbook: default
documentclass: book
date: "`r Sys.Date()`"
---

```{r, include=FALSE}

knitr::opts_chunk$set(message=FALSE, warning=FALSE, echo=FALSE)
options(scipen = 999) ## To disable scientific notation

library(tidyverse)
library(showtext)
## Loading Google fonts (https://fonts.google.com/)
font_add_google("Inconsolata", "inconsolata")
# turn on showtext
showtext_auto()

```

WRANGLERS  

# Corruption Perception Index
## Missing values

```{r, out.width="60%", fig.align="center", fig.cap="Photo by Nathaniel Tetteh on Unsplash"}

masthead <- "assets/nathaniel-tetteh-unsplash.jpg"
knitr::include_graphics(masthead, dpi=72)

```


> Corruption is paid by the poor...\
--- Pope Francis\

***

### Ingest

country, region, CPI score, standard error

```{r echo=TRUE, message=FALSE, warning=FALSE, class.source='fold-hide'}

# Load data
url_root <- "https://raw.githubusercontent.com/UN-AVT/kamino-source/main/sources/0-shared/data/"
url_file <- "corruption-perception-index/corruption-perception-index-2020.csv"
url <- paste0(url_root, url_file)
df <- read.csv(url, header = TRUE)

df

```
### Wrangle

Check the missing values using <kbd>is.na()</kbd> and <kbd>gather()</kbd>

```{r message=FALSE, warning=FALSE, echo=TRUE, class.source = 'fold-show'}

# Count missing values 
f_missing_values <- function(df_missing_values) {
  purrr::map_df(df_missing_values, ~ sum(is.na(.))) %>%
    gather('Column name', 'Count of missing values')
}

# Call the function with your values to the parameters
f_missing_values(df)

```

### References
#### citations for narrative and data sources

* Narrative sources: Luis Freites, [GO](https://github.com/luisfrein/R_Tidytuesday){target="_blank"}
* Data sources: Transparency International, [GO](https://www.transparency.org/en){target="_blank"}
