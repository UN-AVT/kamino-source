# Required libraries
library(tidyverse)

####################################################################################
# Find dimensions
f_dimensions <- function(df_dimensions) {
  dim(df_dimensions)
}

# Call the function with your values to the parameters
f_dimensions(df)
####################################################################################
