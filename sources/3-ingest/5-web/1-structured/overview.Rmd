---
title: "Kamino"
output:
  bookdown::gitbook: default
documentclass: book
date: "`r Sys.Date()`"
---

```{r, include=FALSE}

knitr::opts_chunk$set(message=FALSE, warning=FALSE, echo=FALSE)
options(scipen=999)  # turn-off scientific notation like 1e+48

library(showtext)
## Loading Google fonts (https://fonts.google.com/)
font_add_google("Inconsolata", "inconsolata")
# turn on showtext
showtext_auto()

```

INGEST  


# Ingest via Web Scraping

```{r, out.width="40%", fig.align="center", fig.cap="Photo by Vectorstall, PK"}
# image of an icon to represent the activity or any method (visit for icon https://thenounproject.com/search/?q=attribute&i=4326168)
masthead <- "assets/web.png"
knitr::include_graphics(masthead, dpi=72)
```

When we can't download datasets directly from the public websites are interested in, we can use different ways to scrape data. For example, we can manually copy and paste, or use text pattern matching method, use an API interface, or DOM parsing. 
